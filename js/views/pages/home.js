import Component from '../component.js';

class Home extends Component {
    render() {
        return new Promise(resolve => {
            resolve(`
                
            `);
        });
    }
}

export default Home;