import Component from '../component.js';

class Vr extends Component {
    render() {
        return new Promise(resolve => {
            resolve(`
                
            `);
        });
    }
}

export default Vr;